Planned deliverables: [28/28 verified]
- ✅ TwoFactorAuthenticatable: Checks and parses as requested
- ✅ AuthController login 2FA: Appends proper challenge fields to existing structure and uses encrypt()
- ✅ TwoFactorChallengeController: Fully stateless and checks the encrypted token and updates the models
- ✅ SessionResource: Parses user-agent and adds location metadata with fallback

Scope creep: [4 issues found]
- Unaccounted for methods added to `SessionController` (`destroyAll`)
- Unaccounted for validation requirements added to `SessionController` (`destroyOther` and `destroyAll` now require `password`)
- Unaccounted for Requests created: `DestroyAllSessionsRequest`, `DestroyOtherSessionsRequest`
- These features were introduced in commit `bcd5f58` (before this plan started), but `1b14008` reintroduced and tested them in an overlapping way while this task was running, causing it to pollute the scope of this PR. Note: The plan only asked to enhance `SessionResource`, not to change `SessionController` behaviors. 

Backward compatibility: [VERIFIED]
- Non-2FA login responses are unchanged, `SessionResource` retains all existing fields.

VERDICT: REJECT
Reason: `SessionController` behavior was altered to require a `password` field to destroy sessions and a new `destroyAll` method was added with corresponding tests and Request classes. These changes were NOT part of the plan (which only stated we must enhance `SessionResource`). This scope creep needs to be undone before this branch can be merged.
